# PacketView Backend Configuration

# Server Configuration
PORT=3001
NODE_ENV=development

# Network Configuration
DISABLE_AUTO_CAPTURE=false
DISABLE_PACKET_CAPTURE=false

# Development Settings
DEBUG=false